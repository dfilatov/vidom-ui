import { Button } from 'vidom-components';

export default function LinkExample() {
    return (
        <Button
            type="link"
            theme="islands"
            size="m"
            url=""
            text="Click me"
        />
    );
}
